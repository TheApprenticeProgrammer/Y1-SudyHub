-- Avec BETWEEN

SELECT s.SNAME
FROM S s
WHERE s.STATUS NOT BETWEEN 15 AND 25;

-- Sans BETWEEN

SELECT s.SNAME
FROM S s
WHERE s.STATUS < 15 OR s.STATUS > 25;

-- De Morgan

SELECT s.SNAME
FROM S s
WHERE NOT(s.STATUS >= 15 AND s.STATUS <= 25);
